initSidebarItems({"mod":[["mut_types","Types that can traverse the graph and may modify its structure. These types are backed by a mutable borrow of graph data, so multiple active (unborrowed) instances cannot exist."],["nav_types","Types that can traverse the graph and cannot modify its structure. These types are backed by a read-only borrow of graph data, so multiple instances of them may coexist."]],"trait":[["BoundedIterator","When iterating over components of a graph structure, we need to give a name to the borrow of the underlying graph. This trait does so."],["GraphTypes","Base types that the graph defines a structure over."]]});